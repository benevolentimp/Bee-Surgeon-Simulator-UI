/// @description Default global values
// Properties:
width = 280;
height = 80;

text = "New Game";

// Other variables:
hover = 0; // Gradually changes to 1 if mouse over button

// Script:
script = -1; // Function call for main